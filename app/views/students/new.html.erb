<h1> Student Form </h1>

<%= form_for @student do |f| %>
	<%=f.label :first_name%>
	<%=f.text_field :first_name%>

	<%= f.label :first_name%>
	<%= f.text_field :last_name%>
	<%= submit_tag 'Submit Student' %>
<% end %>
